name: pushed
description: Scoped dependencies for go_router routes using get_it. Manage route-specific dependencies with automatic lifecycle management.
version: 0.2.1
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  equatable: ^2.0.8
  flutter:
    sdk: flutter
  get_it: ^9.2.1
  go_router: ^17.1.0

dev_dependencies:
  coverage: ^1.6.0
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.0
  test: ^1.24.0
  very_good_analysis: ^10.2.0
